---
review_id: apis-r1
artifact: design/04-apis.md
phase: 2-apis
round: 1
reviewed_on: 2026-06-16
reviewer: user
status: approved
approved_in: pending
---

# Review — 04-apis.md (Phase 2 — APIs & contracts) r1

## Outcome

Approved as drafted. User: "Good to go on 04-apis.md." The proposed CLI command/flag names are accepted as the v1 surface.

## Scope reviewed

`04-apis.md`: the CLI contract (REPL + one-shot; subcommands `resume`/`sessions`/`memory`/`config`; flags incl. `--attach`/`--permission-mode`/`--profile`; in-REPL slash-commands `/compact`,`/remember`,`/attach`,`/mode`; §1.5 multimodal attach; §1.6 output + exit codes); the Bedrock Converse boundary in prose (pointing to ADR-0001/0011); 12 tool contracts with Class R/X; the web-delegate contract (ADR-0008); the on-disk contracts for the Operator.

## Decisions accepted (names + minor flags)

- **CLI surface names accepted as proposed:** binary `codingagent`; subcommands `resume`, `sessions`, `memory`, `config`; one-shot `-p/--prompt`; in-REPL slash-commands. May still be revisited at Phase 4/5, but baselined now.
- **Flag 1 — named-command exposure** (separate tool entries vs. annotated `run_command`): **deferred to Phase 4** (task-level detail).
- **Flag 2 — `memory edit`/`rm` subcommands** alongside hand-editing: **kept** (CLI discoverability + direct edit both available).

## Comments

| # | Severity | Comment | Resolution |
|---|----------|---------|------------|
| — | — | No comments — approved as drafted. | — |

## Flagged for later phases

- **Phase 4** pins named-command exposure shape (§3 note) and confirms the CLI command set against the task breakdown.
- **Phase 5** confirms the exact `claude -p` flags + sandbox mechanism for the web delegate (§4, ADR-0008).
- **Phase 3** (`06-formal/`) carries the JSON schemas for tool inputs/results, events, config, and `cli-exit-codes.md`.
- Next: `05-operations.md` — the final Phase 2 artifact (build, run, observability, remediation, packaging); closes Phase 2.
