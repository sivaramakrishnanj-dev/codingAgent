---
review_id: nfrs-1c-r1
artifact: design/00-requirements.md
phase: 1c-nfrs
round: 1
reviewed_on: 2026-06-14
reviewer: user
status: approved
approved_in: e03b032
---

# Review — 00-requirements.md (Phase 1c — NFRs) r1

## Outcome

Approved with one user-directed addition (AWS credential resolution). User: "Good to go, plus we will use AWS Profile based credentials. And in case profile is not found, then would switch to default credential chain." **Phase 1 closes** with this approval.

## Scope reviewed

Phase 1c of `00-requirements.md`: all 7 symbolic NFRs pinned, operational NFRs across 8 themes (Platform, Model/Bedrock, Permission, Context, Verify, Network, Logging, Testing), and the NFR→AC coverage check. Presented with three reviewer-flagged points (Opus cost, heap default, runtime/product tag).

## User decisions captured (via question prompt)

| NFR | Value chosen |
|-----|--------------|
| `NFR-MODEL-DEFAULT` | Claude Opus 4.x (exact Bedrock id deferred to Phase 2 ADR) |
| `NFR-VERIFY-MAX-ITERATIONS` | 5 |
| `NFR-SUBAGENT-MAX` | 1 (configurable to N) |
| `NFR-CONTEXT-COMPACT-THRESHOLD` | 0.85 × effective window |

## User-directed addition (this approval)

AWS credential resolution: **named-profile-first with fallback to the default credential chain.** Folded in as:
- **RD-11** (behavioral default) — named-profile-first; profile-not-found → AWS default provider chain; neither → fail.
- **AC-8.6 / AC-8.7 / AC-8.8** under US-8 — resolve-from-profile / fall-back-on-not-found / exit-`4`-when-no-usable-credentials.
- **NFR-AWS-CREDENTIALS** in the Model/Bedrock NFR group — pins the resolution order and reaffirms read/invoke-only Bedrock access (no AWS write verbs), consistent with the user's global production-safety rule.

This is additive and contradicts no prior 1a/1b decision.

## Comments

| # | Severity | Comment | Resolution |
|---|----------|---------|------------|
| — | — | No comments beyond the AWS-credentials addition above — otherwise approved as drafted. | — |

## Reviewer-flagged points (raised at draft)

| # | Point | Disposition |
|---|-------|-------------|
| 1 | Opus 4.x default is costly for a many-call loop; `NFR-MODEL-SUBAGENT` lets sub-agents drop to a cheaper model. | Accepted by user — Opus default stands; sub-agent model override retained. |
| 2 | `NFR-PLAT-MEM` = 1 GB heap is a generous guess; configurable. | Accepted as configurable default. |
| 3 | Added `[runtime]` vs `[product]` NFR tagging. | Retained — separates shipped-agent behavior from agent-codebase quality. |

## Flagged for later phases

- **Phase 2 ADR (engine/SDK)** must pin the exact Bedrock model id for `NFR-MODEL-DEFAULT` after WebFetch verification, and document the AWS SDK v2 credential-provider wiring for `NFR-AWS-CREDENTIALS` (named profile → `ProfileCredentialsProvider`, fallback → `DefaultCredentialsProvider`).
- **Phase 2 permission ADR** owns the full `NFR-PERMISSION-DENYLIST` and the RD-1 normalized-prefix matching algorithm.
- **Phase 3** `06-formal/cli-exit-codes.md` formalizes the exit-code seed including the new exit-`4` no-credentials path.
