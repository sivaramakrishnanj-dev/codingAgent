---
review_id: adr-batch1-r1
artifact: design/adr/0001-0004 (+ 0000-template)
phase: 2-architecture
round: 1
reviewed_on: 2026-06-15
reviewer: user
status: approved
approved_in: pending
---

# Review — ADR batch 1 (foundational four + template) r1

## Outcome

Approved as drafted. User: "Good to go on batch 1." ADRs 0001–0004 move to `status: accepted`.

This is the first of two ADR batches within the open `2-architecture` sub-phase. Batch 2 (ADRs 0005–0012) follows; the sub-phase resolves when all ADRs land.

## Scope reviewed

- `adr/0000-template.md` — ADR shape + traceability front-matter.
- `adr/0001-engine-sdk-converse-owned-loop.md` — AWS SDK v2 `bedrockruntime` + Converse, owned loop; Opus 4.8 default; Responses/LangChain4j/Spring AI rejected.
- `adr/0002-model-provider-capability-layer.md` — `ModelCapabilityProfile` registry, feature-detected, Claude-only-v1 thin seam (OQ-J).
- `adr/0003-command-execution-spine.md` — two-layer command model, structured result, process-tree-kill timeout, output→disposal (US-20, RD-10).
- `adr/0004-permission-model.md` — 4 modes, Class R/X, RD-1 normalized-prefix match algorithm, RD-2 destructive denylist (OQ-E).

## Comments

| # | Severity | Comment | Resolution |
|---|----------|---------|------------|
| — | — | No comments — approved as drafted. | — |

## Reviewer-flagged points (raised at draft, accepted by user)

| # | Point | Disposition |
|---|-------|-------------|
| 1 | ADR-0004 denylist is heuristic — false positives + never provably complete; stronger guarantee needs OS sandbox (OOS). | **Accept heuristic for v1** — conservative matching, config-extensible, `UNRESTRICTED` still prompts denylisted ops as backstop. |
| 2 | `sudo` + pipe-to-shell always prompt even in `UNRESTRICTED`. | **Keep denylisted** — privilege escalation / RCE deserve a stop regardless of mode. |
| 3 | ADR-0001 pins "newest Opus (4.8)" but defers exact id to runtime/config + impl-time confirm (moved 4.6→4.7→4.8 during design). | **Accepted** — "newest Opus, pinned at implementation." |

## Decisions of record (OQ resolutions in this batch)

- **OQ-J** → ADR-0002: capability profile registry, feature-detection, graceful degradation, Claude-only-v1.
- **OQ-E** → ADR-0004: RD-1 match = tool + executable + subcommand (high-blast tools) / executable (others) / directory-subtree (writes); RD-2 = concrete 15-row destructive denylist, always-prompt + never-remembered.

## Flagged for later phases

- **Batch 2** (ADRs 0005–0012): persistence/event-sourcing, compaction (OQ-D), memory (OQ-F), delegation (Responses alt), config (OQ-G), sub-agents (OQ-C), credentials, greenfield-workflow (OQ-B).
- **Phase 3** may formalize the RD-2 denylist + RD-1 match algorithm as contract-tested artifacts with positive/negative fixtures.
- ADR-0001 SDK coordinate (`bedrockruntime:2.46.10`) and default model id to be confirmed at Phase 5 implementation.
