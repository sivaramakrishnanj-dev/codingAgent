# Editor / vault config
.obsidian/

# Eclipse / JDT IDE artifacts (not part of any task; source of phantom classpath diagnostics)
.classpath
.project
.settings/

# OS noise
.DS_Store

# Java / Maven build output (relevant once Phase 5 code lands)
target/
*.class

# Local agent state (the tool stores sessions/memory under ~/.codingagent, but guard against project-local stray state)
.codingagent/
